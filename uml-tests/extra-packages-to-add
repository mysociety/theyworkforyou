
# Ignore this, pulled in by installed dependencies:

# apache2

# We won't need these on the UML machine:

autofs

bind9
bind9-doc

clamav
clamav-base
clamav-daemon
clamav-freshclam

cron-apt

# I've installed an alternative version of exim:

exim4-daemon-heavy

# Not really relevant on a test installation:

fail2ban
geoip-bin

# Might as well add these:

gnuplot
iotop
libapache2-mod-rpaf
libarchive-tar-perl
libcache-memcached-perl
libclone-perl
libcrypt-openssl-bignum-perl
libcrypt-openssl-dsa-perl
libcrypt-openssl-rsa-perl
libdigest-bubblebabble-perl
libdigest-sha-perl
libgeography-nationalgrid-perl
libio-socket-ssl-perl
libio-string-perl
libnetaddr-ip-perl
libnet-dns-perl
libnet-dns-sec-perl
libnet-dns-zonefile-fast-perl
libpcre3-dev
libregexp-common-dns-perl
libregexp-common-perl
libstatistics-distributions-perl
libsys-hostname-long-perl
libsys-mmap-perl
libunix-mknod-perl 
libxml-rss-perl
locate
mutt
nfs-common
ntp
perl-doc
perlmagick
php5-gd
php5-mcrypt
php5-mhash
php5-mysql 
php5-pgsql
rng-tools

# Provided by util-linux:

schedutils

# Might be useful, so install:

screen

# Don't need squid:

squid3

# Already in the base image:

ssh

# Add these:

strace
subversion-tools
sudo

# Ignore this, sysklogd provides it:

syslogd

# Might as well install these too:

sysstat
tmpreaper
tree
